{
    "layer": "top",
    "modules-left": ["hyprland/workspaces", "hyprland/submap"],
    "modules-center": ["hyprland/window"],
    "modules-right": ["wireplumber", "battery", "custom/power","bluetooth", "network", "clock", "custom/vpn"],
    "hyprland/window": {
        "max-length": 50
    },
    "network": {
	"format": "{ifname}",
	"format-wifi": "{essid} ({signalStrength}%)",
	"tooltip-format-wifi": "{ipaddr}/{cidr}",
	"format-ethernet": "{ifname}",
	"tooltip-format-ethernet": "{ipaddr}/{cidr}",
	"format-disconnected": "",
    },
    "bluetooth": {
	// "controller": "controller1", // specify the alias of the controller if there are more than 1 on the system
	"format": " BT: {status} ",
	"format-disabled": "", // an empty format will hide the module
	"format-connected": " BT: {num_connections} connected ",
	"tooltip-format": "{controller_alias}\t{controller_address}",
	"tooltip-format-connected": "{controller_alias}\t{controller_address}\n\n{device_enumerate}",
	"tooltip-format-enumerate-connected": "{device_alias}\t{device_address}"
    },
    "custom/power": {
	"exec": "sh ~/.config/waybar/power.sh",
	"interval": 2,
	"format": "PWR: {}W"
    },
    "wireplumber": {
	"format": "VOL: {volume}%",
    },
    "battery": {
        "format": "BAT: {capacity}%",
    },
    "clock": {
        "format-alt": "{:%a, %d. %b  %H:%M}"
    },
    "custom/vpn": {
        "exec": "sh ~/.config/waybar/vpn.sh",
        "interval": 5,
        "tooltip": false
    }
}
